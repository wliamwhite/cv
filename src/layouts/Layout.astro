<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content="Liam White — Software developer at Wipfli LLP. AI orchestration, ERP/CRM consulting, and independent software portfolio." />
		<meta name="author" content="Liam White" />
		<meta property="og:title" content="Liam White" />
		<meta property="og:description" content="Software developer — AI orchestration, ERP/CRM consulting, independent software portfolio." />
		<meta property="og:type" content="website" />
		<title>Liam White — Software Developer</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--bg: #fdfdfb;
		--ink: #111111;
		--ink-soft: #2a2a2a;
		--ink-muted: #5a5a5a;
		--rule: #111111;
		--rule-soft: #c8c6bf;
		--link: #0a2f6b;
	}

	* {
		box-sizing: border-box;
	}

	html,
	body {
		margin: 0;
		padding: 0;
		background: var(--bg);
		color: var(--ink);
		font-family: 'Iowan Old Style', 'Palatino Linotype', Palatino, 'Book Antiqua', Georgia, serif;
		font-size: 17px;
		line-height: 1.55;
	}

	h1,
	h2,
	h3,
	h4 {
		font-family: inherit;
		font-weight: 700;
		color: var(--ink);
		margin: 0;
	}

	a {
		color: var(--link);
		text-decoration: underline;
		text-underline-offset: 2px;
		text-decoration-thickness: 1px;
	}

	a:hover {
		color: var(--ink);
		background: #fff1a8;
	}

	code,
	.mono {
		font-family: ui-monospace, 'SF Mono', Menlo, Consolas, 'DejaVu Sans Mono', monospace;
		font-size: 0.92em;
	}

	::selection {
		background: #fff1a8;
		color: var(--ink);
	}
</style>
